"""The Ox Tracker: a local, read-only usage tracker for AI subscriptions."""

# The one place the version number lives. packaging/build.ps1 reads it and
# stamps it into the app manifest and the installer; nothing else sets it.
__version__ = "0.2.0"
